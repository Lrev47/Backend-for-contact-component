

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

//contactInfo

model ContactInfo {
  id              Int     @id @default(autoincrement())
  Name       String
  email           String  @unique
  message    String
}